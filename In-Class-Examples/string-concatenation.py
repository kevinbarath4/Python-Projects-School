a = "string in a double quote"		
b = 'string in a single quote'
c = a + ' concatenated with ' + b		# concatenates values and saves result into a variable

print(c)                                # printing the variable storing the final string