age = 70

# The string in quotes is the template, ie. the string we want to format; it has a {}, a placeholder   
# We are calling format() method on that template string.
# The argument (variable age) we are passing to format() method 
# substitutes the placeholder in the template     
print("I am {} years old.".format(age))
