def addNumbers(num1, num2):
    sum = num1 + num2

    return sum
    
    # the function returns the result of the addition that was saved into a variable
    #return sum
    

def addNumbers(num1, num2):
        
    # the function returns the result of the expression; no variable used to save the result
    return num1 + num2




def addNumbers(num1, num2):
    sum = num1 + num2
    
    # the function returns the return value of the built in round function
    # (the result of the addition rounded to 2 decimal places)
    return round(sum, 2)


def addNumbers(num1, num2):
    sum = round(num1 + num2, 2)
    
    # the function returns the return value of the built in round function
    # (the result of the addition rounded to 2 decimal places)
    return sum


def addNumbers(num1, num2):
    #sum = round(num1 + num2, 2)
    
    # the function returns the return value of the built in round function
    # (the result of the addition rounded to 2 decimal places)
    return round(num1 + num2, 2)